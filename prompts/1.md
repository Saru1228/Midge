0. 项目简介（Project Overview）

本项目旨在建立一个 群体行为 × 场论 × RG × 最大熵（MEM） 的统一建模体系，用于分析并模拟 昆虫群（特别是 midges） 的真实三维动力学。

目标不是复制已有模型，而是探索以下科学问题：

群体行为能否用场论（field theory）描述？

昆虫群是否存在 universality class？是否有 RG 固定点？

在 coarse-grained 视角下，哪些动力学变量是主要的 hydrodynamic DOF？

拓扑 vs metric interactions 哪种适合 midges？

jerk（加加速度）是否是昆虫的核心行为调整机制？

基于 Sinhuber et al. (2019) 的 3D 轨迹数据，构建：

轨迹 → 粗粒化 → 连续场 → hydrodynamic observables → 最大熵推断 → RG → PDE

的完整 pipeline。

1. 数据说明（Data Description）

每一条记录包含以下字段：

id
x, y, z
t
vx, vy, vz
ax, ay, az
jx, jy, jz
speed
accel
jerk


这些字段来自：

3D 位置（x,y,z）

速度（v = dx/dt）

加速度（a = dv/dt）

jerk（j = da/dt）

通过滑动时间差分计算，LOESS / SG filter 平滑

df1_p、df2_p 是经过平滑、中心化后的版本。

df_win 是从时间范围中选取的窗口，用于统计分析。

2. 项目代码结构（Project Structure）
swarm/
│
├── io.py
│     - 数据读取、批读取 Obxx.txt
│
├── kinematics.py
│     - v, a, j 的计算（有限差分）
│     - speed, accel, jerk magnitude
│
├── gaussian_coarse_graining.py
│     - 使用 KDTree 的 Gaussian coarse-grain（支持多尺度 sigma）
│     - 输出 rho, v-field, a-field, j-field
│
├── coarse_grain.py
│     - histogramdd coarse-grain（不推荐大分辨率）
│     - grid 构建工具
│
├── hydrodynamic_derivatives.py
│     - ∇v, curl(v), curl(j) 等
│     - 可用于声模检测
│
├── stats.py
│     - g(r)
│     - S(q)
│     - velocity correlation function
│     - Voronoi volumes
│
├── analysis.py
│     - 声模检测
│     - topological vs metric neighbor analysis
│
└── utils.py
      - 时间窗口切割
      - 中心化
      - 可视化工具


所有核心模型调用都会基于这些模块。

3. 研究进度与步骤（Completed Steps）

以下是项目已经完成的主要科学步骤。

✔ Step 1. 基础统计分析（Statistical Physics）

已完成：

g(r) pair correlation

S(q) 密度结构因子

Voronoi volume 分布（Feng & Ouellette 2023 对比）

边界效应检测

速度相关函数 C_v(r)

结果显示：

midges 在密度 sector 上接近气体（Poisson-like）

无明显 metric 结构 → 进一步暗示 topological interaction

✔ Step 2. 粗粒化（Coarse-graining）

两种方法均已实现：

(A) histogramdd coarse-grain

能产生 density、velocity fields

但高分辨率会导致内存爆炸

(B) Gaussian KDTree coarse-grain（推荐）

已完全实现：

多尺度 sigma

rho, v, a, j fields（3D 网格）

z-slice 可视化

多切片 GIF 动态展示

这是后续 hydrodynamics + PDE 的核心框架。

✔ Step 3. Hydrodynamic Observables

已实现：

C_v(r)

S(q)

声模检测（S(q,ω)）

声模结果：

midges 在当前数据中 没有清晰线性色散 ω=cq

与鸟群（Attanasi 2014）不同

midges 不是 Toner–Tu 型 ordered flock

更像 noisy disordered cluster with persistent topo response

这为你提出的新 universality class 打下基础。

✔ Step 4. 最大熵模型（Maximum Entropy, MEM）

实现并验证：

✔ Topological MEM

最佳邻居数 nc = 9

拟合 J* 与 C_data 一致

pseudo log-likelihood 稳定

✔ Metric MEM

需大 R 才能产生合理 degree

log-likelihood 与 topological 相似，但不优于它

✔ 结论

midges 并非纯 metric；
topological 规则 更能解释真实数据。

4. 今日重点成果（Step 5.1 Scaling）

今天你完成的关键突破：

✔ jerk variance scaling（拓扑尺度 k）

通过时间窗口（≈1s, N≈9700点）：

⟨
𝑗
2
⟩
(
𝑘
)
∼
𝑘
−
1.686
⟨j
2
⟩(k)∼k
−1.686
✔ acceleration variance scaling
⟨
𝑎
2
⟩
(
𝑘
)
∼
𝑘
−
1.65
⟨a
2
⟩(k)∼k
−1.65
✔ velocity variance scaling

趋势正确但略嘈杂。

✨ 最关键的科学发现

acceleration 和 jerk 都在 topological coarse-graining 下显示干净的幂律缩放（α≈1.65–1.70），而 metric 缩放几乎为零。

意味着：

midge 的动力学由拓扑结构支配，而非物理距离。

jerk 不是噪声，而是行为调整的真实物理变量。

acceleration 和 jerk 属于同一个 universality class。

midges 不属于 Toner–Tu universality（metric-based）。

你现在有证据支持 一个全新的 "topological inertial response universality"。

5. 下一步：Step 5.2（Real-space RG）

这是 scaling 后的自然下一步。

我们将：

✔ 使用不同 k 的 coarse-grained fields

计算：

variance(k)

gradient energy(k)

effective parameters (D(k), γ(k))

✔ 画 RG 流
𝑘
 
∂
𝐷
∂
𝑘
,
𝑘
 
∂
𝛾
∂
𝑘
k
∂k
∂D
	​

,k
∂k
∂γ
	​

✔ 寻找 fixed point

是否存在：

jerk-driven universality

acceleration-damped universality

或 Reynolds-type jerk fixed point

这是你的最有可能发表的核心贡献之一。

6. Step 5.3 PDE 建模计划（Acceleration-based Field Theory）

由于 acceleration 和 jerk scaling 都很好，建议：

使用 acceleration 作为主场变量：

∂
𝑡
𝑎
=
𝐷
𝑎
∇
2
𝑎
−
𝛾
𝑎
+
𝜂
∂
t
	​

a=D
a
	​

∇
2
a−γa+η

jerk 为：

𝑗
=
∂
𝑡
𝑎
j=∂
t
	​

a

结合 scaling 与 RG，可拟合：

D_a

γ

噪声强度 Δ

这将形成 midges 的 新 hydrodynamic field theory。

7. GPT 使用规范（Prompt Standards）

为了在新的对话框中保持一致行为，应加入：

回答必须遵循 coarse-grain → field → RG 的逻辑

保持用户对“场 = 3D 矢量场驱动粒子”的直觉

解释必须映射到

Toner–Tu（速度场）

Bialek（涨落场、MEM）

Reynolds（jerk 行为响应）

代码应适配 swarm/ 下的模块结构

给出的数学表达式最好同时附提供 latex 版本

对 scaling / RG / PDE 的讨论需要使用物理直觉、不要给纯数学推导

8. 总结：项目目前状态
已完成：

数据清洗

3D 粗粒化（Gaussian）

hydrodynamic observables

S(q), S(q,ω) 声模

MEM

jerk/acc topo scaling（重大成果）

正在进行：

Step 5.2 Real-space RG

下一步：

acceleration PDE 建模

jerk correction terms

universality class 判定